name: snapchatads.campaigns
type: ingestr
connection: gcp-default

parameters:
  source_connection: my-snapchatads
  source_table: campaigns_stats:TOTAL:impressions,spend,swipes
  destination: bigquery

