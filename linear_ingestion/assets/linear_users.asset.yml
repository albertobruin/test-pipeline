name: linear.users
type: ingestr
description: Ingest users from Linear workspace

parameters:
  source_connection: linear_connection
  source_table: 'users'
  destination: bigquery
  incremental_strategy: merge
  incremental_key: updatedAt

columns:
  - name: id
    type: STRING
    description: Unique identifier for the user
    primary_key: true

tags:
  - linear
  - users
  - source