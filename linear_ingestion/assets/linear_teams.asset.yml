name: linear.teams
type: ingestr
description: Ingest teams from Linear workspace

parameters:
  source_connection: linear_connection
  source_table: 'teams'
  destination: bigquery
  incremental_strategy: merge
  incremental_key: updatedAt

columns:
  - name: id
    type: STRING
    description: Unique identifier for the team
    primary_key: true

tags:
  - linear
  - teams
  - source